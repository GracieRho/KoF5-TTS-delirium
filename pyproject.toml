[project]
name = "kof5-tts-delirium"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1,<1",
    "uvicorn>=0.52.4,<1",
]

[dependency-groups]
dev = ["httpx>=0.28,<1"]
